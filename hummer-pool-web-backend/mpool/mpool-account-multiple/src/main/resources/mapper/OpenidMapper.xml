<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mpool.accountmultiple.program.mapper.OPenidMapper">

	<delete id="deleteBinding">
		DELETE FROM `account_openid`
		WHERE  `openid`= #{openid}
	</delete>

	<!--<insert id="addOPenid">-->
		<!--INSERT INTO `account_openid` (`userid`, `addtime`, `updatetime`)-->
		<!--VALUES(#{userid}, #{addtime}, #{updatetime})-->
	<!--</insert>-->



	<!--<update id="subtractionTotalDue">-->
		<!--UPDATE `user_pay` SET `total_due`=total_due - #{totalDue} WHERE `id`=#{id}-->
	<!--</update>-->

</mapper>
